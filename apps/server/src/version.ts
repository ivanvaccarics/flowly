export const SERVER_VERSION = "0.1.0";

/** Persisted entity format shared with the canonical contracts. */
export const VAULT_FORMAT_VERSION = 1;

/** Transaction CSV and complete portable archive format. */
export const EXPORT_FORMAT_VERSION = 1;
